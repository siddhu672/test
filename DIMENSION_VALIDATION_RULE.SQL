create TABLE STG.DIMENSION_VALIDATION_RULE (
	DIMENSION_VALIDATION_RULE_ID NUMBER(38,0) NOT NULL autoincrement,
	MARKET VARCHAR(10) NOT NULL,
	PLATFORM_NAME VARCHAR(100) NOT NULL,
	PLATFORM_ID NUMBER(38,0),
	FEED_NAME VARCHAR(150) NOT NULL,
	FEED_ID NUMBER(38,0),
	COLUMN_NAME VARCHAR(100) NOT NULL,
	LOGICAL_COLUMN_NAME VARCHAR(100) NOT NULL,
	RULE_FORMAT VARCHAR(1000) NOT NULL,
	ACTUAL_PLATFORM_RULE_FORMAT VARCHAR(1000),
	MANDATORY_COLUMNS NUMBER(38,0) NOT NULL,
	ID_COLUMN_NAME VARCHAR(50) NOT NULL,
	ISACTIVE BOOLEAN NOT NULL,
	RULE_VERSION VARCHAR(50) NOT NULL DEFAULT 'VERSION 0',
	CREATED_DATE TIMESTAMP_TZ(9) NOT NULL DEFAULT CURRENT_TIMESTAMP(),
	CREATED_BY VARCHAR(255) NOT NULL DEFAULT CURRENT_USER(),
	UPDATED_DATE TIMESTAMP_TZ(9) NOT NULL DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_BY VARCHAR(255) NOT NULL DEFAULT CURRENT_USER(),
	constraint PK_DIMENSION_VALIDATION_RULE primary key (DIMENSION_VALIDATION_RULE_ID)
);
